const http = require('http');

exports.apifunctions = function() {
    const http = require('http');

function http(req, res) {
    http.createServer((req, res) => {
    if (req.method === 'GET') {
        res.end('Send this')
    }
    
    if (req.method === 'POST') {
    }
});
}
server.listen(3000);
}